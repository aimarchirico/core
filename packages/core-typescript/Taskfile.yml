version: '3'

tasks:
  build:
    cmds:
      - echo "No build step for typescript config"
  check:
    cmds:
      - echo "Checking config format"
  fix:
    cmds:
      - echo "No fix needed for typescript config"
  run:
    cmds:
      - echo "No run command for typescript config"
