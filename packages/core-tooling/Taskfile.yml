version: '3'

tasks:
  build:
    cmds:
      - echo "No build step for tooling config"
  check:
    cmds:
      - echo "Checking config format"
  fix:
    cmds:
      - echo "No fix needed for tooling config"
  run:
    cmds:
      - echo "No run command for tooling config"
